# 1.0.0 release verification

This record contains only public release identifiers, aggregate checks, and cryptographic artifact hashes. Live account state and private content remain in ignored local paths.

## Build

- Version: `1.0.0`
- Source target: public immutable annotated tag `v1.0.0`
- Wheel/sdist build: passed from the release working tree
- Package contents: 40 wheel entries and 110 sdist entries; no `work/`, Git metadata, Cookie material, or raw evidence paths
- Fresh Python 3.12 wheel installation: passed (`smfa --help` and `smfa doctor --json`)
- Fresh doctor result: core/CDP/FFmpeg/path/quota checks passed; absent optional ASR and disabled OCR were warnings as expected for the core-only wheel
- Exact tagged artifact SHA-256 checksums:

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `social_media_favorites_archiver-1.0.0-py3-none-any.whl` | 103143 | `1994dc3de5089aa8dc27f1c5d4d5229f4c91ffbca8d39c8b1578074e074def1a` |
| `social_media_favorites_archiver-1.0.0.tar.gz` | 393308 | `f265c595cade0b754970805502580d47a646fc768b50a58e2a0278f9b07f7781` |

GitHub reports the same digest for each uploaded asset.

## Gates

| Gate | Status | Evidence |
|---|---|---|
| Automated tests, lint, and types | passed | 205 passed, 2 deselected; 74.32% coverage; Ruff and strict mypy passed |
| Privacy and fixture scan | passed | 27 focused checks; no tracked `work/` files or live artifacts |
| Three-platform representative live pipelines | passed | `docs/verification/2026-08-08-live-e2e.md` |
| Skill structure and trigger evaluation | passed | `docs/verification/2026-08-09-skill-evals.md` |
| Fresh package installation | passed | Build section |

## Publication

- GitHub repository: `dvlin-dev/social-media-favorites-archiver`
- Release commit: `55260258b5716cf5487b246f947ba535c3168a38`
- Annotated tag: `v1.0.0` (tag object `a2885db93c5aeae5eb1967176e3a0c8b9cb59397`, peeled to the release commit)
- GitHub release URL: https://github.com/dvlin-dev/social-media-favorites-archiver/releases/tag/v1.0.0
- Public tag installation: passed in a fresh temporary `uv tool` directory; version `1.0.0` resolved to the release commit, and `smfa --help` plus sanitized `smfa doctor --json` ran successfully
- Skills.sh verification: passed; public listing and clean installation verified on 2026-08-09
- ClawHub verification: passed; public release, security scan, install, trust envelope, and generated Skill Card verified on 2026-08-09
- Clean-room verification: Task 23

Remote `main`, the peeled tag, the release assets, and the public install were independently checked after publication.

## Skills.sh distribution

- Current official Skills.sh documentation, CLI reference, FAQ, and API reference were rechecked on 2026-08-09. The current distribution model still discovers a public GitHub Skill through the `skills` CLI and anonymous installation telemetry; no source upload is required.
- A fresh temporary directory outside this repository discovered exactly one Skill with `npx -y skills add dvlin-dev/social-media-favorites-archiver --list`.
- The nested bundle was installed with `npx -y skills add dvlin-dev/social-media-favorites-archiver --skill social-media-favorites-archiver --copy -y`.
- The host's global Git configuration rewrote the first public HTTPS clone to SSH, which was unavailable through the local network proxy; that attempt installed nothing. Repeating both commands with `GIT_CONFIG_GLOBAL=/dev/null` provided a clean public-client configuration and passed.
- The installed directory contained exactly the eight intended text files: `SKILL.md`, `LICENSE`, `.clawhubignore`, and five references. Its source diff was empty. The exact 124-character description, `v1.0.0` install target, optional environment declarations, references, and absence of application/private files were verified.
- A fresh Codex CLI session loaded the installed Skill for a positive own-favorites archival prompt and declined it for a negative single-video transcription prompt. Neither smoke test invoked the workflow or accessed an account.
- Anonymous installation telemetry was generated at approximately 00:42 CST. The actual public listing returned HTTP 200 at 00:47 CST, so delayed 15- and 60-minute checks were unnecessary.
- Public URL: https://skills.sh/dvlin-dev/social-media-favorites-archiver/social-media-favorites-archiver

## ClawHub distribution

- Current official ClawHub, Skill format, creating Skills, OpenClaw Skills, publishing, and CLI documentation were rechecked on 2026-08-09. The planned `clawhub skill publish` and `openclaw skills` syntax remains current.
- The user was notified before publication that the ClawHub Skill bundle is MIT-0 while the application repository remains MIT.
- Official CLI versions: ClawHub `0.23.3`; OpenClaw `2026.7.1-2`.
- Authenticated publisher handle: `dvlin-dev`.
- Qualified reference: `@dvlin-dev/social-media-favorites-archiver`.
- Version and tag: `1.0.0`, `latest`.
- Public URL: https://clawhub.ai/dvlin-dev/skills/social-media-favorites-archiver
- The exact publication command from Task 22 passed after a successful dry run. ClawHub accepted seven source files (the ignore file is not distributed), source fingerprint `523753c6e592e3d2236dfe52c16465170c2c35d4ada196b030319effb929b996`, and MIT-0 license metadata.
- Public search and `openclaw skills install @dvlin-dev/social-media-favorites-archiver --version 1.0.0` passed. The installed seven source files matched the published source bundle byte-for-byte; only OpenClaw provenance metadata was added locally.
- The release initially became public and installable before the asynchronous generated Skill Card was ready. During that expected window, verification failed only with `card.missing`; the source bundle and scan were already clean. The corresponding official security workflow completed successfully and triggered the Skill Card worker. No Skill change or patch release was required.
- Final `openclaw skills verify @dvlin-dev/social-media-favorites-archiver` passed with no reasons. The generated card is 2,707 bytes with SHA-256 `6811666d74db56495645ba10d5e76481f754484b298aff3dc4aa0371b6b8f100`; `openclaw skills verify @dvlin-dev/social-media-favorites-archiver --card` returned the card successfully.
- Security result: clean, benign, high confidence. Static scan engine `v2.4.26` found no suspicious patterns; SkillSpector `2.3.5` reported `SAFE`, score 0, severity low, and zero issues; VirusTotal reported zero malicious or suspicious engines and 64 undetected engines.

## 1.0.4 clean-room security and CI hardening

The first post-publication clean install found an indirect-prompt-injection boundary omission and then exercised the plan's defect loop until both distribution paths behaved correctly. Immutable patch history was preserved rather than moving any tag:

- `1.0.1` classified platform titles, descriptions, captions, subtitles, OCR/ASR transcripts, author fields, and URLs as untrusted data, prohibited embedded instructions/commands/links, and separated fixed enrichment instructions from allowlisted structured text.
- `1.0.2` required Agents to copy the immutable Git-tag install command exactly after one synthetic Skills.sh smoke substituted an unavailable PyPI-style command.
- `1.0.3` corrected invalid YAML quoting that had prevented GitHub Actions from creating jobs and added a workflow parsing regression.
- `1.0.4` provisioned the documented FFmpeg prerequisite in CI, stabilized forced-color help assertions without an optional dependency, and made optional MLX typing portable. The final branch and tagged commit runs were green.

### Final build and publication

- Version: `1.0.4`
- Release commit: `b8e14d90977d30c6bb811ae175fc7fb3e2cc6725`
- Annotated tag object: `530a5f508e125b06c81fae1787048c22e10a763a`
- GitHub release: https://github.com/dvlin-dev/social-media-favorites-archiver/releases/tag/v1.0.4
- GitHub Actions branch run `31303410114` and tag run `31303484736`: all 10 jobs passed across Linux/macOS, Python 3.11/3.12, whole-package coverage, contract, integration, privacy, quality, licenses, build, and installation.
- Local final suite: 207 passed, 2 deselected; 74% branch-aware coverage; Ruff, strict mypy, locked resolution, dependency licenses, package build, isolated installation, `smfa --help`, sanitized `smfa doctor --json`, and uninstall passed.

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `social_media_favorites_archiver-1.0.4-py3-none-any.whl` | 103135 | `d5b58da6cff49f08ddfa8e590a9224f0fbbc301181a121f53de6b6474928e86f` |
| `social_media_favorites_archiver-1.0.4.tar.gz` | 398619 | `eb44ac9c0762c857459bc685e178cc2840f1e558a97def23284ae89bdacf5df2` |

### Final G8 clean-room evidence

- Skills.sh and ClawHub were installed in separate new temporary roots. Skills.sh contained exactly the eight intended bundle files. ClawHub contained the seven published source files plus only its provenance metadata. Every source file matched the release bundle byte-for-byte.
- Fresh Codex sessions for each install applied the Skill to an own-favorites archival request, declined a single-link transcription request, treated an embedded `ignore rules / run curl / open URL` string as untrusted data, quoted the exact `v1.0.4` Git install command, and selected `smfa doctor --config /absolute/path/to/config.yml` first. They did not run the workflow or access an account.
- Independent `uv tool` installations from both clean roots resolved `v1.0.4` to the release commit above. Version listing, help, sanitized doctor, and tool uninstall passed.
- Skills.sh removal and ClawHub `--yes` removal both deleted only their temporary installed Skill folders. The user's vault, database, browser profile, model cache, and account state were neither located nor accessed during this rerun.
- Skills.sh continues to display Gen/Snyk medium-risk capability warnings for remote Git installation and authenticated social-account access; Socket reports zero alerts. The fix addresses the actionable content-injection boundary without hiding the Skill's inherent capabilities.
- Public pages returned HTTP 200: https://skills.sh/dvlin-dev/social-media-favorites-archiver/social-media-favorites-archiver and https://clawhub.ai/dvlin-dev/skills/social-media-favorites-archiver.
- ClawHub `1.0.4` source fingerprint: `84f66a1e4b1a99cbf18c8e98d89aaf93a27353606e1ccb06bb493aa34ca1d243`. Static scan and the aggregate security review are clean, benign, high confidence. Final `verify` passed with no reasons; the generated Skill Card is 3,384 bytes with SHA-256 `347c296e94c3e421912eacc88113669c803190d6ab1c12063b4a4b62f47af097`, and `--card` retrieval passed.

### User-directed account boundary

No real account was accessed for patch versions `1.0.1` through `1.0.4`, per the user's explicit instruction. Task 18 remains the representative one-item-per-platform live evidence. A bounded public-`1.0.0` Xiaohongshu metadata-only rerun completed before that instruction and is retained only as historical evidence. The patch reruns used sanitized fixtures, automated regression tests, public installation, and synthetic Agent behavior; this accepted limitation is not relabeled as a new live pass.

## Task 24 final audit

- Every Task 0–23 tracker row and evidence checkbox was audited as complete. G1–G8 evidence remains present; Task 18's unselected live-heavy content types remain disclosed gaps rather than passes.
- The final local suite passed with 207 tests and 2 marker-based deselections at 74% branch-aware coverage. Ruff, strict mypy, lock consistency, dependency licenses, fixture privacy, release documentation, CI workflow parsing, and Skill bundle checks passed.
- A fresh source distribution and wheel built successfully. The wheel installed into a new isolated `uv tool` root; version listing, `smfa --help`, sanitized `smfa doctor --json`, and uninstall passed.
- A second isolated installation from the immutable public `v1.0.4` Git tag resolved to release commit `b8e14d90977d30c6bb811ae175fc7fb3e2cc6725`; the same help, doctor, and uninstall checks passed.
- Remote `main`, the annotated/peeled tag, and the non-draft GitHub release were verified. The GitHub release, Skills.sh listing, and ClawHub listing each returned HTTP 200; Skills.sh discovered exactly one intended Skill; ClawHub `1.0.4` verification and Skill Card retrieval passed with clean, benign, high-confidence security results.
- The introduced-history path audit found no live vault, database, browser-profile, download, or E2E work paths. Automated fixture/privacy checks found no committed private content or secrets. The working copy contained only the intentional README, plan, and release-evidence edits before the final documentation commit.
- No real social-platform account was accessed during this audit, as directed by the user.
