# G1 core and G2 privacy verification

Date: 2026-08-08
Scope: automated, synthetic, and local-only gates before live platform validation

## G1 core

Result: **satisfied for entry into Task 18 live validation**.

- `uv run ruff check .`: passed.
- `uv run mypy src`: passed for 35 source files.
- `uv run pytest -q`: 180 passed.
- Unit group, excluding opt-in markers: 87 passed.
- Contract group, excluding opt-in markers: 25 passed.
- Integration group, excluding opt-in markers: 51 passed and 2 heavyweight tests deselected.
- Whole-package branch coverage, excluding `heavyweight` and `live`: 178 passed, 2 deselected, 72.59% total; the CI floor is 70%.
- Coverage targets the entire `social_media_favorites_archiver` package. Core orchestration, storage, safety, adapters, workers, and processors are not omitted from the source set.
- A source distribution and wheel were built in a new temporary directory, installed with dependencies into a new Python 3.12 virtual environment, imported, and invoked through `smfa --help`: passed.
- The reviewed direct-dependency license inventory check passed. Optional installed runtimes were checked when present.

The 70% threshold is evidence-based rather than aspirational: the measured baseline is 72.59%, and lower adapter/backend line coverage corresponds mainly to authenticated browser paths, subprocess/model paths, and defensive error branches. Task 18 must supply real end-to-end evidence; mocks cannot upgrade a platform claim.

## G2 privacy

Result: **satisfied for entry into Task 18 live validation**.

- `tests/test_fixture_privacy.py`: 15 passed.
- Every file below `tests/fixtures/sanitized` is a bounded JSON document with `evidence.values: synthetic-placeholders-only`.
- The automated scan rejects cookies, authorization headers, bearer/key formats, sensitive structural values, signed/auth query parameters, private profile paths, high-entropy opaque values, embedded URL credentials, unapproved hosts, and non-JSON media.
- Deterministic fake adapters cover all three platforms and the planned text/article, video, image/gallery, unavailable, subtitle-present, ASR-fallback, and multi-part shapes without live network access.
- Existing path-containment, redaction, asset-integrity, note-protection, reconciliation, lease-recovery, cleanup-barrier, and text-only enrichment tests remain green in the full suite.
- `SECURITY.md` defines personal-account scope, private vulnerability reporting, redaction expectations, no anti-bot bypass, dedicated-browser state, local-only media, and verified cleanup boundaries.
- `CONTRIBUTING.md` requires test-driven development and synthetic sanitized fixtures.
- Model weights are not redistributed. Runtime code and model-weight licensing are documented separately in `docs/third-party-model-licenses.md`.

Live vaults, databases, browser profiles, media, screenshots, raw responses, captions, private URLs, and signed access data remain prohibited from Git. Task 18 must use ignored local paths and record aliases and aggregate evidence only.

## Remaining gates

G3–G6 require real platform and rerun evidence in Task 18. G7–G8 require Skill evaluation and public distribution evidence in Tasks 19–23. No stable support or publication claim is made by this document.
